test_int_pg:
  steps:
    - create table test_src (rid serial, int_col integer)
    - insert into test_src (int_col) values (0), (3), (NULL)
    - insert into test_src (int_col) values (11), (14)
  config:
    conn: postgres
    table: test_src
    rid: rid
  dst:
    2pg:
      steps:
        - create table test_dst (rid integer, int_col integer)
      config:
        conn: postgres
        table: test_dst
        rid: rid
